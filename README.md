# Zero-to-GANs-with-Pytorch
I try to learn it from the Scratch level.


### Python Environment create
- conda create -n learn python=3.13 -y
- conda activate learn
- pip install -r requirements.txt
` one note that, if you want to create virtual environment and install requirements.txt with this command, you must need pre-install anaconda in your system.`